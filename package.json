{
  "name": "teamscord",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "check": "npm run build && npm run test:ui && npm run test:updates && npm run test:media && npm run test:browser && npm run test:browser:screen && cargo fmt --manifest-path src-tauri/Cargo.toml -- --check && cargo fmt --manifest-path infra/relay/Cargo.toml -- --check && cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings && cargo clippy --manifest-path infra/relay/Cargo.toml --all-targets -- -D warnings && cargo test --manifest-path src-tauri/Cargo.toml && cargo test --manifest-path infra/relay/Cargo.toml",
    "release": "npm run build && tauri build --bundles nsis && powershell -ExecutionPolicy Bypass -File scripts/checksum.ps1",
    "test:ui": "vitest run src/lib/ui-model.test.ts src/lib/notice.test.ts src/lib/qr.test.ts",
    "test:updates": "vitest run src/lib/update-checker.test.ts",
    "test:media": "vitest run src/lib/webrtc.test.ts",
    "test:browser": "node scripts/webrtc-browser-smoke.mjs",
    "test:browser:screen": "node scripts/webrtc-display-permission-smoke.mjs",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.8.0",
    "@types/qrcode": "^1.5.6",
    "jsqr": "^1.4.0",
    "lucide-react": "^0.468.0",
    "qrcode": "^1.5.4",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.4.1",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^3.2.7"
  }
}
